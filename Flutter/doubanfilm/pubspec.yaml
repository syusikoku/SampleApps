name: doubanfilm
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  #
  provider:
  #  scoped_model: ^1.1.0
  #  bloc: ^6.1.3
  flutter_bloc: ^6.1.3
  #  equatable: ^1.2.3
  dio: ^4.0.0
  #
  #  shared_preferences: ^2.0.7

  rxdart:

  # ...其他依赖
  cached_network_image:

  # 瀑布流
  flutter_staggered_grid_view:

  # 列表刷新
  flutter_easyrefresh:

  # 加载动画效果：闪烁
  shimmer:

  flutter_screenutil:

  # 生成闪屏页
  flutter_native_splash:


  # 路由管理
  # https://pub:flutter-iocn/packages/fluro:
  fluro:

  # get框架:状态管理，路由管理
  get:

  # Android与Flutter混合开发:
  #  Android原生界面跳转Flutter界面，并传参
  #  Flutter界面跳转Android原生界面，并传参
  #  Android和Flutter在同一界面显示
  flutter_boost:

  # 响应式编程：web会使用
  #flutter_bootstrap:

  kasax:
    #    version: 0.0.7
    git:
      url: https://gitee.com/syusikoku/flutter_kasax.git

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/resources/